import 'package:flutter/material.dart';


void main() => runApp(const ThermalPrinter());

class ThermalPrinter extends StatelessWidget {
  const ThermalPrinter({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
